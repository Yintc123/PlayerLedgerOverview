# PostgreSQL
POSTGRES_USER=jko
POSTGRES_PASSWORD=replace-with-strong-password
POSTGRES_DB=jko_donation

# Redis
REDIS_PASSWORD=replace-with-strong-password

# Generate strong passwords with:
#   openssl rand -base64 32
